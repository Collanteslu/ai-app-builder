# Entorno de TEST — base de datos aislada (docker-compose.test.yml, puerto 5433).
# Los tests leen DATABASE_URL_TEST; nunca tocan la BD de desarrollo (5432).
DATABASE_URL_TEST=postgresql://user:password@localhost:5433/app_test
NODE_ENV=test
