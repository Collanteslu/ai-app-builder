# Base de datos
DATABASE_URL=postgresql://user:password@localhost:5432/app

# Entorno
NODE_ENV=development

# Sesión (para auth con JWT / cookies)
# SESSION_SECRET=genera_uno_con_openssl_rand_-base64_32
